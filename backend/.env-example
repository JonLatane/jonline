DATABASE_URL=postgres://localhost/jonline_dev
# Separate DB used by `cargo test` (see backend/src/tests/), so specs never touch dev data.
TEST_DATABASE_URL=postgres://localhost/jonline_test

MINIO_ENDPOINT=http://localhost:9000
MINIO_REGION=
MINIO_BUCKET=jonline-dev
MINIO_ACCESS_KEY=ROOTNAME
MINIO_SECRET_KEY=CHANGEME123
