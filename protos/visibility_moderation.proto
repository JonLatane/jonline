syntax = "proto3";

package jonline;

enum Visibility {
  VISIBILITY_UNKNOWN = 0;
  PRIVATE = 1;
  LIMITED = 2;
  SERVER_PUBLIC = 3;
  GLOBAL_PUBLIC = 4;
}

enum Moderation {
  MODERATION_UNKNOWN = 0;
  UNMODERATED = 1;
  PENDING = 2;
  APPROVED = 3;
  REJECTED = 4;
}